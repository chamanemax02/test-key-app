{
  "name": "sonora-lk-backend",
  "version": "1.0.0",
  "description": "Secure proxy between the SONORA LK Android app and the third-party metadata API. Holds the API key server-side; the app never sees it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "express-rate-limit": "^7.4.0",
    "dotenv": "^16.4.5",
    "node-cache": "^5.1.2",
    "helmet": "^7.1.0",
    "cors": "^2.8.5"
  }
}
